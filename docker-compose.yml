version: "3.8"

services:
  collector:
    build: .
    container_name: unified-collector
    restart: unless-stopped
    ports:
      - "8100:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - API_KEY=${API_KEY}
      - DB_PATH=/app/data/collector.db
